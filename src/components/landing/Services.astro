---
const services = [
  {
    category: "Our Services",
    items: [
      "LumiEngage → Real-time customer data & engagement intelligence",
      "LumiOrchestrate → Automated workflows & decision execution",
      "LumiShield → Fraud detection, risk scoring & compliance protection",
      "LumiInsight → Predictive analytics & continuous business optimization",
      "LumiResilience → System health, security & proactive observability",
    ],
  },
];
---

<section>
  <div class="px-8 py-12 max-w-xl mx-auto">
    <h2 class="text-4xl tracking-tight text-base-900 text-balance font-medium">
      Services
    </h2>

    <div class="flex flex-col gap-px mt-4">
      {
        services.map(({ category, items }) => (
          <div class="bg-white p-8 rounded-3xl">
            <ul role="list" class="grid grid-cols-1 gap-y-2 list-disc pl-4">
              {items.map((title) => (
                <li class="text-sm text-base-600">{title}</li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>
  </div>
</section>
